# PermutiveAPI 6.8.3

PermutiveAPI 6.8.3 is a backward-compatible release-integrity patch that synchronizes the published source distribution with the current AI-native platform contract and lean solo-maintainer CI configuration.

## Changed

- Adopt the AI Native Platform v0.2 `security_evidence` contract and retain explicit repository-backed security evidence.
- Pin the registered consumer to the revised MCP-optional v0.2 contract while keeping this repository's MCP surface enabled.
- Keep the simplified solo-maintainer protection model built around the aggregate `ci`, AI-native `validate`, and CodeQL gates while preserving the underlying lint, Python 3.9-3.13 test, package, and security coverage.

## Compatibility

This release does not introduce an incompatible change to the public Python SDK, transports, resources, query DSL, plugins, MCP interface, or governance behavior. It republishes the current repository contract and validation state as an immutable patch release.

## Release integrity

The 6.8.3 candidate must pass the protected CI, AI-native, security, release-metadata, contract, replay, performance, build, clean-wheel, and public-PyPI verification gates. Publication uses PyPI Trusted Publishing and the exact immutable candidate artifacts produced by the release workflow.
