[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name="PermutiveAPI"
version="v5.0.3"
readme = "README.md"
requires-python=">=3.7"
dependencies = ["requests"]