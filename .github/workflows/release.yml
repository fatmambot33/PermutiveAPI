name: Legacy release workflow (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Publication is handled by .github/workflows/python-publish.yml."
